package com.zen.recruitment.discount.strategy

enum class DiscountStrategyType {
    NO_DISCOUNT,
    PERCENTAGE,
    QUANTITY,
    HIGHER,
    LOWER,
    PERCENTAGE_THEN_QUANTITY,
    QUANTITY_THEN_PERCENTAGE
}
